services:
  recruitment-mail-importer:
    build:
      context: .
    image: qicore/recruitment-mail-importer:local
    container_name: qicore-recruitment-mail-importer
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - recruitment-mail-importer-data:/data
    networks:
      - mail-relay
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import os,time; p='/data/heartbeat'; assert os.path.exists(p) and time.time()-os.path.getmtime(p) < 900"
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 90s
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"

networks:
  mail-relay:
    external: true
    name: ${MAIL_NETWORK_NAME:-mail-relay}

volumes:
  recruitment-mail-importer-data:
    name: recruitment-mail-importer-data
